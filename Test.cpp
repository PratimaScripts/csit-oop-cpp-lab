#include <iostream>  // Allows input and output. Gives access to cout and cin
using namespace std; // Lets us use standard names like cout without writing std::cout

// the starting point of every C++ program. Execution begins here. 
int main() {
    cout << "Hello World!" << endl; //endl moves to next line, and flushes output buffer

    return 0;
}